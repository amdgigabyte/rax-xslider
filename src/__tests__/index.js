import {createElement, Component} from 'rax';
import renderer from 'rax-test-renderer';
import XSlider from '../';

describe('XSlider', () => {
  it('test typeof XSlider', () => {
    expect(typeof XSlider).toEqual('function');
  });
});
